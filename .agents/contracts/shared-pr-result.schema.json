{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Shared PR Result Schema",
  "description": "Standard output format for pipeline steps that produce a pull request. Enables cross-pipeline composition for PR-producing workflows.",
  "type": "object",
  "required": ["pr_url", "pr_number", "branch", "summary"],
  "properties": {
    "pr_url": {
      "type": "string",
      "format": "uri",
      "description": "URL to the created or updated pull request"
    },
    "pr_number": {
      "type": "integer",
      "minimum": 1,
      "description": "Pull request number"
    },
    "branch": {
      "type": "string",
      "minLength": 1,
      "description": "Branch name for the pull request"
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "Brief description of what the pull request does"
    },
    "title": {
      "type": "string",
      "description": "Pull request title"
    }
  },
  "additionalProperties": false
}
