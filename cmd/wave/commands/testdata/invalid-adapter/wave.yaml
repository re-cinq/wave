apiVersion: v1
kind: WaveManifest
metadata:
  name: test-invalid-adapter
  description: Test project with invalid adapter reference
adapters:
  claude:
    binary: claude
    mode: headless
    output_format: json
personas:
  navigator:
    adapter: nonexistent  # This adapter does not exist
    system_prompt_file: personas/navigator.md
    temperature: 0.1
    permissions:
      allowed_tools:
        - Read
      deny: []
runtime:
  workspace_root: .wave/workspaces
  default_timeout_minutes: 30
