description: "Code review and quality checks"
temperature: 0.1
permissions:
  allowed_tools:
    - Read
    - Write
    - Glob
    - Grep
    - Bash
  deny:
    - "Edit(*)"
    - "Bash(rm -rf /*)"
    - "Bash(git push*)"
    - "Bash(git commit*)"
