description: "Read-only codebase exploration and analysis"
temperature: 0.1
permissions:
  allowed_tools:
    - Read
    - Write
    - Glob
    - Grep
    - "Bash(git log*)"
    - "Bash(git status*)"
  deny:
    - "Edit(*)"
    - "Bash(git commit*)"
    - "Bash(git push*)"
