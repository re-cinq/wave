description: "Work supervision and quality evaluation"
temperature: 0.2
permissions:
  allowed_tools:
    - Read
    - Glob
    - Grep
    - Bash
  deny:
    - "Write(*)"
    - "Edit(*)"
    - "Bash(git push*)"
    - "Bash(git commit*)"
    - "Bash(rm*)"
