description: "Deep codebase research and analysis"
temperature: 0.1
permissions:
  allowed_tools:
    - Read
    - Glob
    - Grep
    - WebSearch
    - WebFetch
    - Write
  deny:
    - "Edit(*)"
    - "Bash(*)"
    - "Write(*.go)"
    - "Write(*.ts)"
    - "Write(*.py)"
