description: "Structured synthesis of analysis findings into actionable JSON proposals"
model: sonnet
temperature: 0.2
permissions:
  allowed_tools:
    - Read
    - Glob
    - Grep
  deny:
    - "Edit(*)"
    - "Bash(*)"
