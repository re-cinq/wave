description: "Systematic debugging and root cause analysis"
temperature: 0.1
permissions:
  allowed_tools:
    - Read
    - Glob
    - Grep
    - Bash
  deny:
    - "Write(*)"
    - "Edit(*)"
    - "Bash(rm -rf /*)"
    - "Bash(git push*)"
    - "Bash(git commit*)"
