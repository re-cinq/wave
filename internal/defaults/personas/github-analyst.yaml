description: "GitHub issue analysis and scanning"
temperature: 0.1
permissions:
  allowed_tools:
    - Read
    - "Bash(gh issue view*)"
    - "Bash(gh issue list*)"
    - "Bash(gh --version)"
    - "Bash(git log*)"
    - "Bash(git status*)"
    - "Bash(ls *)"
    - "Write(.wave/artifact.json)"
  deny:
    - "Bash(gh issue edit*)"
    - "Bash(gh issue create*)"
    - "Bash(gh issue close*)"
    - "Bash(glab *)"
    - "Bash(tea *)"
    - "Bash(bb *)"
    - "Edit(*)"
