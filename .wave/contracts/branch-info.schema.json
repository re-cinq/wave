{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "description": "Information about the created feature branch",
  "required": [
    "branch_name",
    "base_branch",
    "status"
  ],
  "properties": {
    "branch_name": {
      "type": "string",
      "description": "Name of the created feature branch"
    },
    "base_branch": {
      "type": "string",
      "description": "Name of the branch it was created from"
    },
    "status": {
      "type": "string",
      "enum": [
        "created",
        "failed"
      ],
      "description": "Status of the branch creation operation"
    },
    "message": {
      "type": "string",
      "description": "Additional details or error message"
    }
  }
}