description: "Context compaction for relay handoffs"
temperature: 0.0
permissions:
  allowed_tools:
    - Read
    - Write
    - Edit
    - Bash
    - Glob
    - Grep
  deny:
    - "Edit(*)"
    - "Bash(rm*)"
    - "Bash(git push*)"
    - "Bash(git commit*)"
