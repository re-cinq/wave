description: "Work supervision and quality evaluation"
model: claude-haiku
temperature: 0.2
permissions:
  allowed_tools:
    - Read
    - Write
    - Glob
    - Grep
    - Bash
  deny:
    - "Edit(*)"
    - "Bash(git push*)"
    - "Bash(git commit*)"
    - "Bash(rm*)"
