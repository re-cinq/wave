description: "Creative challenger for divergent thinking and complexity hunting"
temperature: 0.8
permissions:
  allowed_tools:
    - Read
    - Write
    - Glob
    - Grep
    - "Bash(wc *)"
    - "Bash(git log*)"
    - "Bash(git diff*)"
    - "Bash(find*)"
    - "Bash(ls*)"
  deny:
    - "Edit(*)"
    - "Bash(git commit*)"
    - "Bash(git push*)"
    - "Bash(rm*)"
