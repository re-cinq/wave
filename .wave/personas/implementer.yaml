description: "Execution specialist for code changes and structured output"
model: claude-opus
temperature: 0.3
permissions:
  allowed_tools:
    - Read
    - Write
    - Edit
    - Bash
    - Glob
    - Grep
  deny:
    - "Bash(rm -rf /*)"
    - "Bash(sudo *)"
