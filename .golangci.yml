version: "2"

linters:
  default: standard
  enable:
    - unparam
    - wastedassign
    - gocritic
    - nolintlint
  settings:
    nolintlint:
      require-explanation: true
      require-specific: true
  exclusions:
    presets:
      - comments
      - std-error-handling
