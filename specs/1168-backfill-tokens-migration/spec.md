# scope: move webui backfillRunTokens out of NewServer constructor into a versioned migration

**Issue:** [#1168](https://github.com/re-cinq/wave/issues/1168)
**Repository:** re-cinq/wave
**Labels:** scope-audit
**State:** OPEN
**Author:** nextlevelshit

## Context

From wave-scope-audit run wave-scope-audit-20260422-075324-5b6c.

`webui` refactor action from scope. `backfillRunTokens` currently runs inside `NewServer`, conflating construction with a one-shot data migration. Migrations belong in a versioned migration path, not a constructor.

## Acceptance Criteria

- [ ] `backfillRunTokens` removed from `NewServer`
- [ ] Logic moved into a versioned migration within the state/migration layer
- [ ] Migration runs idempotently and only when needed
- [ ] Server construction has no data-mutation side effects

---
*Generated by wave-scope-audit pipeline*
