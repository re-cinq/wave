# scope: break tui↔doctor↔onboarding import cycle by extracting shared types into internal/suggest

**Issue:** [#1160](https://github.com/re-cinq/wave/issues/1160)
**Repository:** re-cinq/wave
**State:** OPEN
**Author:** nextlevelshit
**Labels:** scope-audit

## Issue Body

### Context

From wave-scope-audit run wave-scope-audit-20260422-075324-5b6c.

Scope action on `internal/suggest`: "promote shared types here to break tui↔doctor cycle". The current tui/doctor/onboarding packages form an import cycle that blocks clean layering.

### Acceptance Criteria

- [ ] Shared types moved into `internal/suggest`
- [ ] Import cycle between `internal/tui`, `internal/doctor`, `internal/onboarding` eliminated (verified with `go vet` / `go build ./...`)
- [ ] No duplicate type definitions remain in the three packages

---

## Current Edge Map (verified via `go list`)

- `internal/suggest` → `internal/doctor` (engine.go uses `doctor.Report`, `doctor.CodebaseHealth`)
- `internal/doctor` → `internal/onboarding` (types.go default `isOnboarded` calls `onboarding.IsOnboarded`)
- `internal/onboarding` → `internal/tui` (steps.go, ontology_step.go embed tui widgets)
- `internal/tui` → none of {doctor, onboarding, suggest}

No active cycle today (`go build ./...` passes), but tui has its own duplicate copy of the doctor health-check types, which signals the latent cycle that scope-audit flagged.

## Duplicate Type Inventory

| Concept | doctor | tui |
|---------|--------|-----|
| Severity enum | `Status` (StatusOK/Warn/Err) | `HealthCheckStatus` (HealthCheckOK/Warn/Err/Checking) |
| Check outcome | `CheckResult{Name,Category,Status,Message,Fix}` | `HealthCheckResultMsg{Name,Status,Message,Details}` |

External callers:
- `cmd/wave/commands/doctor.go` — uses `doctor.Status*`, `doctor.Report`
- `internal/webui/handlers_health.go` — uses `tui.HealthCheckStatus`, `tui.NewDefaultHealthDataProvider`
- `internal/suggest/engine.go` + tests — use `doctor.Report`, `doctor.CodebaseHealth`

## Definition of Done

- `Status`, `CheckResult`, `Report`, `CodebaseHealth`, `PRSummary`, `IssueSummary`, `CIStatus` live in `internal/suggest`.
- `internal/suggest` no longer imports `internal/doctor`.
- `internal/tui` no longer defines `HealthCheckStatus` or `HealthCheckResultMsg` — uses `suggest` types (or thin aliases).
- `doctor → onboarding` edge severed (or kept harmless) so `tui → suggest`, `doctor → suggest`, `onboarding → tui` graph stays acyclic.
- `go build ./...` and `go vet ./...` clean.
- All existing tests green; no behavioral change.

## Origin

Generated by wave-scope-audit pipeline, run id `wave-scope-audit-20260422-075324-5b6c`.
