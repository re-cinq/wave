# scope: consolidate four duration humanizers into internal/humanize and delete webui/display duplicates

**Issue:** [#1163](https://github.com/re-cinq/wave/issues/1163)
**Repository:** re-cinq/wave
**Author:** nextlevelshit
**Labels:** scope-audit
**State:** OPEN

## Context

From wave-scope-audit run wave-scope-audit-20260422-075324-5b6c.

Package inventory: `humanize` — `keep (absorb duplicates)`, `display` — `keep (merge duplicate FormatDuration)`. Four separate duration humanizers exist across the codebase. Consolidate into `internal/humanize`.

## Acceptance Criteria

- [ ] Single duration humanizer lives in `internal/humanize`
- [ ] Duplicates in `internal/webui` and `internal/display` deleted
- [ ] All callers updated to use `internal/humanize`
- [ ] Unit tests cover the consolidated implementation

---

## Inventory of Duration Humanizers (verified)

| # | Location | Signature | Output style |
|---|----------|-----------|--------------|
| 1 | `internal/humanize/humanize.go:14` | `Duration(d time.Duration) string` | `-`, `5s`, `3m15s`, `2h30m` |
| 2 | `internal/display/formatter.go:456` | `FormatDuration(durationMs int64) string` | `123ms`, `5s`, `3m 15s`, `2h 30m` |
| 3 | `internal/webui/embed.go:307` | `formatDuration(seconds float64) string` (template helper, HTML-escaped) | `<1s` (escaped), `5s`, `1m 5s` |
| 4 | `internal/webui/handlers_runs.go:1037` | `formatDurationValue(d time.Duration) string` | `5s`, `2m`, `2m 15s`, `1h`, `1h 15m` |

The `formatDurationMs` (`cmd/wave/commands/retro.go:296`), `formatDuration` in `cmd/wave/commands/list.go:1641`, `internal/audit/logger.go:159`, and `internal/tui/pipeline_list.go:834` are out of scope for this issue (issue text names only `internal/webui` and `internal/display`); they will be re-audited later but may be folded in if the consolidated API trivially covers them.

*Generated by wave-scope-audit pipeline*
