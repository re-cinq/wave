# scope: split internal/state.StateStore into domain-scoped interfaces (RunStore, EventStore, OntologyStore, WebhookStore, ChatStore)

## Source

- Issue: [re-cinq/wave#1159](https://github.com/re-cinq/wave/issues/1159)
- Author: nextlevelshit
- Labels: `scope-audit`
- State: OPEN

## Context

From wave-scope-audit run wave-scope-audit-20260422-075324-5b6c.

Package inventory flags `internal/state` as `active (bloated)` with a monolithic `StateStore` interface. Scope action: split into domain-scoped interfaces so consumers depend only on the store surface they use.

## Acceptance Criteria

- [ ] `StateStore` split into `RunStore`, `EventStore`, `OntologyStore`, `WebhookStore`, `ChatStore`
- [ ] SQLite implementation continues to satisfy all new interfaces
- [ ] Callers updated to depend on narrow interfaces, not the aggregate
- [ ] No change to on-disk schema required

## Notes

- `internal/state/store.go` defines the monolithic `StateStore` interface (~80 methods, ~3000 lines).
- Single concrete implementation: `stateStore` (read-write via `NewStateStore`, read-only via `NewReadOnlyStateStore`).
- ~41 production + test files reference `state.StateStore`.
- Migration of callers must preserve ISP: each consumer depends only on the surface it uses.

---

*Generated by wave-scope-audit pipeline*
