# scope: deduplicate docker and skill checks between internal/doctor and internal/preflight

**Issue**: [#1171](https://github.com/re-cinq/wave/issues/1171)
**Repository**: re-cinq/wave
**Labels**: scope-audit
**State**: OPEN
**Author**: nextlevelshit

## Context

From wave-scope-audit run wave-scope-audit-20260422-075324-5b6c.

Package inventory: `doctor` — `keep (dedupe vs preflight)`, `preflight` — `keep (dedupe vs doctor)`. Docker and skill checks exist in both packages with drift risk.

## Acceptance Criteria

- [ ] Docker check has a single implementation shared between `internal/doctor` and `internal/preflight`
- [ ] Skill check has a single implementation shared between the two
- [ ] Neither package duplicates the other's check logic
- [ ] Tests cover the shared check

---
*Generated by wave-scope-audit pipeline*

## Current State (2026-04-26)

- `internal/preflight/preflight.go`
  - `Checker.CheckDockerDaemon()` — `LookPath("docker")` + `runCmd("docker", "info")`.
  - `Checker.CheckSkills()` + `isSkillInstalled()` / `isSkillInstalledWithToolBin()` — runs `sh -c cfg.Check`, with optional auto-install via `cfg.Install`.
- `internal/doctor/checks_deps.go`
  - `checkRequiredSkills()` — runs `sh -c check` per skill (no install). Duplicates the install-probe core of preflight.
- `internal/doctor` has **no** Docker daemon check today. Only `internal/doctor/profile.go::detectDocker(dir)` (Dockerfile/compose file presence) — a different concern (project profile, not host capability).
- Drift risk is real for skills (two `sh -c check` paths). For Docker daemon, the audit anticipates drift the moment doctor adds its own health check.
