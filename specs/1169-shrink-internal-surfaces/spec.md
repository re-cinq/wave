# scope: shrink exported surfaces of internal/bench, internal/continuous, internal/classify (lore), internal/cost, internal/sandbox, internal/attention

**Issue:** [#1169](https://github.com/re-cinq/wave/issues/1169)
**Repository:** re-cinq/wave
**Author:** nextlevelshit
**State:** OPEN
**Labels:** scope-audit

## Context

From wave-scope-audit run wave-scope-audit-20260422-075324-5b6c.

Package inventory marks these as partial with `shrink API` / `un-export internals` / `un-export concrete types` actions: `bench`, `continuous`, `classify` (remove lore subsystem), `cost`, `sandbox` (also drop unused backend constants), `attention`.

## Acceptance Criteria

- [ ] `internal/bench` internals un-exported; public surface limited to what external callers need
- [ ] `internal/continuous` API shrunk to the continuous-run abstraction; helpers un-exported
- [ ] `classify.LoreProvider` and associated lore subsystem removed (per "Wave IS NOT a memory/lore platform")
- [ ] `internal/cost` helpers un-exported
- [ ] `internal/sandbox` concrete types un-exported; unused backend constants dropped
- [ ] `internal/attention` API shrunk
- [ ] External behavior unchanged

---
*Generated by wave-scope-audit pipeline*
