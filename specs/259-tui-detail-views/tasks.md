# Tasks: TUI Alternative Master-Detail Views

**Feature**: #259 — TUI Alternative Master-Detail Views  
**Branch**: `259-tui-detail-views`  
**Generated**: 2026-03-06  
**Spec**: `specs/259-tui-detail-views/spec.md`  
**Plan**: `specs/259-tui-detail-views/plan.md`

## Phase 1: View Infrastructure (Setup & Foundational)

Core view switching mechanism: ViewType enum, Tab cycling, ContentModel refactoring, status bar updates.

- [X] T001 P1 US1 Create `internal/tui/views.go` — define `ViewType` enum (`ViewPipelines`, `ViewPersonas`, `ViewContracts`, `ViewSkills`, `ViewHealth`), `ViewType.String()` method returning display names ("Pipelines", "Personas", etc.), `ViewChangedMsg{View ViewType}` message type, and `PipelineStepRef{PipelineName, StepID string}` shared struct used by persona/contract/skill views for pipeline usage cross-references.

- [X] T002 P1 US1 Modify `internal/tui/content.go` — add `currentView ViewType` field (default `ViewPipelines`) to `ContentModel`. Add pointer fields for alternative view models (`personaList *PersonaListModel`, `personaDetail *PersonaDetailModel`, `contractList *ContractListModel`, `contractDetail *ContractDetailModel`, `skillList *SkillListModel`, `skillDetail *SkillDetailModel`, `healthList *HealthListModel`, `healthDetail *HealthDetailModel`). Add provider fields (`personaProvider PersonaDataProvider`, `contractProvider ContractDataProvider`, `skillProvider SkillDataProvider`, `healthProvider HealthDataProvider`). Add `IsFiltering() bool` method that delegates to the active view's list model filtering state. Add `CurrentFocus() FocusPane` method returning `m.focus`. Update `NewContentModel()` to accept a `ContentProviders` struct containing the four alternative view providers (use struct to avoid long param list).

- [X] T003 P1 US1 Modify `internal/tui/content.go` — implement Tab key interception in `ContentModel.Update()`. Insert Tab check *before* the existing `tea.KeyMsg` switch at line 61 (before focus-based child routing). Logic: if `msg.Type == tea.KeyTab`: (a) if `currentView == ViewPipelines && m.detail.paneState == stateConfiguring`, forward Tab to `m.detail` for form field navigation; (b) otherwise, call `m.cycleView()`. Implement `cycleView()` helper: increment `currentView` modulo 5, reset `m.focus` to `FocusPaneLeft`, set active list focused and detail unfocused, lazy-init new view models if nil (create them with providers and call SetSize), return `tea.Batch(ViewChangedMsg{newView}, FocusChangedMsg{FocusPaneLeft}, initCmd)` where `initCmd` is the new view's Init() if just created.

- [X] T004 P1 US1 Modify `internal/tui/content.go` — update `View()` to switch on `currentView`: for `ViewPipelines` use existing `m.list.View()` + `m.detail.View()`; for each alternative view use the corresponding `m.<view>List.View()` + `m.<view>Detail.View()` (show "Select a [view item] to view details" placeholder if view model is nil). Apply same left pane dimming logic when `m.focus == FocusPaneRight`. Update `SetSize()` to propagate dimensions to all non-nil view models. Update message routing in `Update()`: route `PersonaDataMsg`, `PersonaSelectedMsg`, `PersonaStatsMsg` to persona models; route `ContractDataMsg`, `ContractSelectedMsg` to contract models; route `SkillDataMsg`, `SkillSelectedMsg` to skill models; route `HealthCheckResultMsg`, `HealthSelectedMsg` to health models. Pipeline-specific messages continue routing only to pipeline models.

- [X] T005 P1 US1 Modify `internal/tui/app.go` — change quit guard at line 58 from `!m.content.list.filtering && m.content.focus == FocusPaneLeft` to `!m.content.IsFiltering() && m.content.CurrentFocus() == FocusPaneLeft`. Add `ViewChangedMsg` to the status bar forwarding switch at line 94 alongside `FocusChangedMsg`, `FormActiveMsg`, `LiveOutputActiveMsg`, `FinishedDetailActiveMsg`.

- [X] T006 P1 US6 Modify `internal/tui/statusbar.go` — add `currentView ViewType` field. Handle `ViewChangedMsg` in `Update()`: set `m.currentView = msg.View` and `m.contextLabel = msg.View.String()`. Update hint logic in `View()`: after existing formActive/liveOutputActive/finishedDetailActive checks, add view-specific hints. For Health view left pane: `"↑↓: navigate  Enter: view  r: recheck  Tab: view  q: quit  ctrl+c: exit"`. For Personas/Contracts/Skills left pane: `"↑↓: navigate  Enter: view  /: filter  Tab: view  q: quit  ctrl+c: exit"`. For any alternative view right pane: `"↑↓: scroll  Esc: back  Tab: view  q: quit  ctrl+c: exit"`. Pipelines view keeps existing hints unchanged.

- [X] T007 P1 US1 Add tests to `internal/tui/content_test.go` — test Tab cycles through all 5 views in order (SC-001): send 5 Tab key messages, assert `currentView` transitions Pipelines→Personas→Contracts→Skills→Health→Pipelines. Test Tab wraps from Health back to Pipelines. Test Tab forwards to form when pipeline detail is in `stateConfiguring` (SC-011): set up pipeline detail with active form, send Tab, assert no view change. Test Tab from right pane resets focus to left pane (US1.4): set focus to right, send Tab, assert focus is FocusPaneLeft and view changed. Test view state preservation (SC-002): initialize persona view, set cursor to index 2, tab away and back, assert cursor is still 2. Test `IsFiltering()` delegates to active view's list.

- [X] T008 P1 US1 Add tests to `internal/tui/app_test.go` — test `q` quits from alternative views when not filtering. Test `q` does NOT quit when filtering is active in alternative view. Test `ViewChangedMsg` forwarding to status bar.

- [X] T009 P1 US6 Add tests to `internal/tui/statusbar_test.go` — test `ViewChangedMsg` updates `contextLabel` to correct view name (SC-009). Test Health view shows "r: recheck" hint. Test alternative view right pane shows scroll/back hints.

## Phase 2: Personas View (User Story 2)

Persona list, detail, data provider, and tests.

- [X] T010 [P] P1 US2 Create `internal/tui/persona_provider.go` — define `PersonaInfo` struct (Name, Description, Adapter, Model string; AllowedTools, DeniedTools []string; PipelineUsage []PipelineStepRef). Define `PersonaStats` struct (TotalRuns, SuccessfulRuns int; AvgDurationMs int64; LastRunAt time.Time). Define `PersonaDataProvider` interface with `FetchPersonas() ([]PersonaInfo, error)` and `FetchPersonaStats(name string) (*PersonaStats, error)`. Implement `DefaultPersonaDataProvider` with constructor taking `*manifest.Manifest` and `state.StateStore` (nullable) and `pipelinesDir string`. `FetchPersonas()`: iterate `manifest.Personas`, sort alphabetically, build PersonaInfo for each with name/description/adapter/model/permissions; scan all pipeline YAML files via `DiscoverPipelines`-style iteration to build PipelineUsage mapping (for each step, if step.Persona matches, add PipelineStepRef). `FetchPersonaStats()`: if store is nil return nil; call `store.GetRecentPerformanceHistory(PerformanceQueryOptions{Persona: name, Limit: 1000})`; aggregate: count total, count where Success==true, average DurationMs, max StartedAt.

- [X] T011 [P] P1 US2 Create `internal/tui/persona_list.go` — define `PersonaDataMsg{Personas []PersonaInfo, Err error}` and `PersonaSelectedMsg{Name string, Index int}` messages. Implement `PersonaListModel` following `PipelineListModel` pattern: fields for width/height, items []PersonaInfo, cursor int, navigable []PersonaInfo (filtered subset), filtering bool, filterInput textinput.Model, filterQuery string, focused bool, scrollOffset int, provider PersonaDataProvider, loaded bool. `NewPersonaListModel(provider)` constructor. `Init()` returns async `fetchPersonaData` command. `Update()` handles `PersonaDataMsg` (store items, build navigable, emit initial PersonaSelectedMsg), `tea.KeyMsg` (↑/↓ navigation, `/` filter toggle, Enter emits nothing — Enter handled by ContentModel for focus switch), `PersonaSelectedMsg` pass-through. `View()` renders alphabetically sorted persona names with `"▶ "` cursor indicator, supports scroll offset, shows "No personas configured" empty state. `SetSize()`, `SetFocused()`, `buildNavigableItems()`, `adjustScrollOffset()` follow pipeline_list.go patterns.

- [X] T012 [P] P1 US2 Create `internal/tui/persona_detail.go` — define `PersonaStatsMsg{Name string, Stats *PersonaStats, Err error}` message. Implement `PersonaDetailModel` with two states: empty (no selection) and showing detail. Fields: width/height, focused bool, viewport viewport.Model, selected *PersonaInfo, stats *PersonaStats, statsLoading bool, provider PersonaDataProvider. `NewPersonaDetailModel(provider)` constructor. `Init()` returns nil. `Update()` handles: `PersonaSelectedMsg` — set selected to matching PersonaInfo, trigger async stats fetch, update viewport content; `PersonaStatsMsg` — set stats, update viewport. `SetSize()` updates viewport dimensions. `View()` returns centered placeholder when empty, or viewport view when showing detail. `renderPersonaDetail(info *PersonaInfo, stats *PersonaStats, width int) string` renders: title (bold cyan), description, adapter, model (if set), allowed tools list, denied tools list, pipeline usage (list of "pipeline / step" pairs), run stats section (total runs, success rate %, avg duration formatted via `formatDuration`, last run timestamp) or "No runs recorded" if stats is nil. Use same lipgloss styling as `renderAvailableDetail`.

- [X] T013 P1 US2 Create `internal/tui/persona_provider_test.go` — test `DefaultPersonaDataProvider.FetchPersonas()`: create temp dir with pipeline YAML files containing persona references, create mock manifest with known personas, verify returned PersonaInfo entries are sorted alphabetically, have correct fields, and correct PipelineUsage. Test `FetchPersonaStats()`: create in-memory state store, insert known PerformanceMetricRecord entries for a persona, verify aggregated stats (total, successful, avg duration, last run). Test `FetchPersonaStats()` with nil store returns nil. Test `FetchPersonaStats()` with no records returns nil.

- [X] T014 P1 US2 Create `internal/tui/persona_test.go` — test `PersonaListModel`: mock provider returns 4 personas → verify list renders all 4 names alphabetically. Test cursor navigation ↑/↓. Test filter `/` → type query → verify navigable subset. Test empty persona list shows placeholder. Test `PersonaDetailModel`: mock provider returns known persona and stats → verify detail renders name, description, permissions, pipeline usage, run stats. Test no stats shows "No runs recorded". Test viewport scrolling.

## Phase 3: Contracts View (User Story 3)

Contract list, detail, data provider, and tests.

- [X] T015 [P] P2 US3 Create `internal/tui/contract_provider.go` — define `ContractInfo` struct (Label, Type, SchemaPath, Source, SchemaPreview string; PipelineUsage []PipelineStepRef). Define `ContractDataProvider` interface with `FetchContracts() ([]ContractInfo, error)`. Implement `DefaultContractDataProvider` with constructor taking `pipelinesDir string`. `FetchContracts()`: scan all pipeline YAML files, parse pipeline.Pipeline structs. For each step with `Handover.Contract.Type != ""`: if SchemaPath is set, use schema filename as label, read first ~30 lines from file as SchemaPreview; if SchemaPath is empty and Source is set, use `"pipeline:step"` as label, use Source as preview. Deduplicate by SchemaPath: merge PipelineUsage refs for same schema. Sort alphabetically by label. Return []ContractInfo.

- [X] T016 [P] P2 US3 Create `internal/tui/contract_list.go` — define `ContractDataMsg{Contracts []ContractInfo, Err error}` and `ContractSelectedMsg{Label string, Index int}` messages. Implement `ContractListModel` following same pattern as PersonaListModel but with ContractInfo items. Renders contract label with type badge (e.g., `[json_schema]`). Supports ↑/↓ navigation, `/` filter. Shows "No contracts configured" when empty.

- [X] T017 [P] P2 US3 Create `internal/tui/contract_detail.go` — implement `ContractDetailModel` with empty and showing-detail states. On `ContractSelectedMsg`: update viewport with rendered content. `renderContractDetail()` shows: label (bold cyan), type, schema file path (if set), schema preview (rendered in dimmed style, truncated to ~30 lines with note if truncated), pipeline usage list ("pipeline / step" pairs). Use viewport for scrolling.

- [X] T018 P2 US3 Create `internal/tui/contract_provider_test.go` — test deduplication by schema path with combined usage (SC-005). Test inline contracts appear with `"pipeline:step"` label. Test schema preview loaded from file (create temp schema file). Test empty contracts returns empty slice.

- [X] T019 P2 US3 Create `internal/tui/contract_test.go` — test `ContractListModel` with mock data: 3 contracts → verify list renders all 3. Test type badge rendering. Test filter. Test `ContractDetailModel`: select contract → verify detail shows type, path, preview, usage. Test empty state placeholder.

## Phase 4: Skills View (User Story 4)

Skill list, detail, data provider, and tests.

- [X] T020 [P] P2 US4 Create `internal/tui/skill_provider.go` — define `SkillInfo` struct (Name, CommandsGlob string; CommandFiles []string; InstallCmd, CheckCmd string; PipelineUsage []string). Define `SkillDataProvider` interface with `FetchSkills() ([]SkillInfo, error)`. Implement `DefaultSkillDataProvider` with constructor taking `pipelinesDir string`. `FetchSkills()`: scan all pipeline YAML files. For each pipeline with `Requires.Skills`, extract skill name and SkillConfig. Deduplicate by skill name, aggregate pipeline names for PipelineUsage. For each skill, resolve CommandsGlob via `filepath.Glob()` to populate CommandFiles. Sort alphabetically by name.

- [X] T021 [P] P2 US4 Create `internal/tui/skill_list.go` — define `SkillDataMsg{Skills []SkillInfo, Err error}` and `SkillSelectedMsg{Name string, Index int}` messages. Implement `SkillListModel` following same pattern as PersonaListModel with SkillInfo items. Renders skill name with command count badge (e.g., `(3 cmds)`). Supports ↑/↓ navigation, `/` filter. Shows "No skills configured" when empty.

- [X] T022 [P] P2 US4 Create `internal/tui/skill_detail.go` — implement `SkillDetailModel` with empty and showing-detail states. On `SkillSelectedMsg`: update viewport. `renderSkillDetail()` shows: name (bold cyan), commands glob path, discovered command files list (or "No commands found" if empty), install command, check command, pipeline usage list. Use viewport for scrolling.

- [X] T023 P2 US4 Create `internal/tui/skill_provider_test.go` — test skills deduplicated across pipelines (SC-006). Test glob resolution discovers command files (create temp command files matching glob). Test missing command files returns empty CommandFiles. Test empty skills returns empty slice.

- [X] T024 P2 US4 Create `internal/tui/skill_test.go` — test `SkillListModel` with mock data: 2 skills → verify list renders both with command count. Test filter. Test `SkillDetailModel`: select skill → verify detail shows glob, commands, install/check, usage. Test empty state placeholder.

## Phase 5: Health View (User Story 5)

Health list, detail, data provider with 6 diagnostic checks, and tests.

- [X] T025 [P] P2 US5 Create `internal/tui/health_provider.go` — define `HealthCheckStatus` enum (`HealthOK`, `HealthWarn`, `HealthErr`, `HealthChecking`). Define `HealthCheck` struct (Name, Description string; Status HealthCheckStatus; Message string; Details map[string]string; LastChecked time.Time). Define `HealthCheckResultMsg` (Name string; Status HealthCheckStatus; Message string; Details map[string]string). Define `HealthDataProvider` interface with `RunCheck(name string) HealthCheckResultMsg` and `CheckNames() []string`. Implement `DefaultHealthDataProvider` with constructor taking `*manifest.Manifest`, `state.StateStore`, `pipelinesDir string`. Implement 6 checks: (1) "Git Repository" — `exec.Command("git", "rev-parse", "--is-inside-work-tree")`, branch via `git rev-parse --abbrev-ref HEAD`, remote via `git remote get-url origin`, dirty via `git status --porcelain`; (2) "Adapter Binary" — `exec.LookPath` for each adapter binary in manifest.Adapters; (3) "SQLite Database" — attempt `store.ListRuns(ListRunsOptions{Limit:1})`; (4) "Wave Configuration" — report persona/pipeline/adapter counts from manifest; (5) "Required Tools" — `exec.LookPath` for each tool across all pipeline Requires.Tools; (6) "Required Skills" — run skill check commands from pipeline Requires.Skills.

- [X] T026 [P] P2 US5 Create `internal/tui/health_list.go` — define `HealthSelectedMsg{Name string, Index int}` message. Implement `HealthListModel` with fixed list (6 entries, no section headers). Fields: width/height, checks []HealthCheck (initialized with all checks in `HealthChecking` state), cursor int, focused bool, scrollOffset int, provider HealthDataProvider. `Init()` returns batch of 6 async `RunCheck` commands via `tea.Cmd` closures. `Update()` handles: `HealthCheckResultMsg` — update matching check's status/message/details/lastChecked; `tea.KeyMsg` — ↑/↓ navigation, `r` key emits batch of 6 re-check commands, Enter emits `HealthSelectedMsg`. No filtering support. `View()` renders each check with status icon: `●` for OK (green, lipgloss color "2"), `▲` for WARN (yellow, color "3"), `✗` for FAIL (red, color "1"), `…` for Checking (gray, color "244"). Shows `"▶ "` cursor indicator.

- [X] T027 [P] P2 US5 Create `internal/tui/health_detail.go` — implement `HealthDetailModel` with empty and showing-detail states. Fields: width/height, focused bool, viewport viewport.Model, selected *HealthCheck. On `HealthSelectedMsg`: find matching check, update viewport. On `HealthCheckResultMsg`: if matches selected check, re-render viewport. `renderHealthDetail()` shows: check name (bold cyan), status with icon and color, message, detail key-value pairs (label: value format), last checked timestamp formatted as "2006-01-02 15:04:05". Use viewport for scrolling.

- [X] T028 P2 US5 Create `internal/tui/health_provider_test.go` — test each of the 6 health checks with mocked dependencies. Test `CheckNames()` returns all 6 names. Test git check returns OK for valid repo, FAIL for non-repo. Test adapter check returns OK when binary found, FAIL when missing. Test database check returns OK when store responds, FAIL when nil store.

- [X] T029 P2 US5 Create `internal/tui/health_test.go` — test `HealthListModel`: mock provider returns known results → verify status icons render correctly (SC-007). Test `r` key triggers re-check batch (SC-008). Test initial state shows all checks as "checking…". Test cursor navigation. Test `HealthDetailModel`: select check → verify detail renders name, status, details, timestamp.

## Phase 6: Integration Wiring (Cross-Cutting)

Wire alternative view providers into RunTUI and ensure all views work end-to-end.

- [X] T030 P1 US1 Modify `internal/tui/app.go` `RunTUI()` function — construct `DefaultPersonaDataProvider`, `DefaultContractDataProvider`, `DefaultSkillDataProvider`, `DefaultHealthDataProvider` from `deps.Manifest`, `deps.Store`, `deps.PipelinesDir`. Pass them to `NewContentModel()` via the `ContentProviders` struct added in T002. Also update `NewAppModel()` signature to accept `ContentProviders` and forward to `NewContentModel()`.

- [X] T031 P1 US1 Modify `internal/tui/content.go` — in the `Update()` method, add key routing for Enter and Esc in alternative views. When `currentView != ViewPipelines` and `msg.Type == tea.KeyEnter` and `m.focus == FocusPaneLeft`: switch focus to right pane, set active list unfocused and detail focused, emit `FocusChangedMsg{Right}`. When `msg.Type == tea.KeyEscape` and `m.focus == FocusPaneRight` and `currentView != ViewPipelines`: switch focus to left, set active list focused and detail unfocused, emit `FocusChangedMsg{Left}`. Also handle `'c'` key: only process cancel for `ViewPipelines` (skip for alternative views). Handle `/` filter: for alternative views when `m.focus == FocusPaneLeft`, forward to active list model.

- [X] T032 P1 US1 Verify existing tests still pass — run `go test ./internal/tui/...` and `go test ./...` to confirm no regressions (SC-010). Fix any compilation errors from the new types, modified signatures, or message routing changes.

## Phase 7: Polish & Cross-Cutting Concerns (User Story 6)

Final integration verification and edge case handling.

- [X] T033 P3 US6 Add integration-style tests to `internal/tui/content_test.go` — test full navigation flow: Tab to Personas, select persona with Enter, verify right pane focused, press Esc to go back to left pane, Tab to Contracts, verify view switched (SC-012). Test `q` quits from Personas view. Test `q` does NOT quit when filtering in Skills view. Test rapid Tab cycling (5 Tabs) returns to Pipelines.

- [X] T034 P3 US6 Handle edge cases in alternative views — ensure empty states work: manifest with no personas shows "No personas configured" placeholder in PersonaListModel; no contracts → "No contracts configured"; no skills → "No skills configured". Ensure health checks continue running when user tabs away (checks are async commands, model retained in memory per C3/C8). Ensure `Ctrl-C` triggers graceful shutdown from any view (already handled at AppModel level, no changes needed — just verify in tests).
