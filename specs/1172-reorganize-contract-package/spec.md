# scope: reorganize internal/contract by contract type rather than by operation

**Issue:** [#1172](https://github.com/re-cinq/wave/issues/1172)
**Repository:** re-cinq/wave
**Labels:** scope-audit
**State:** OPEN
**Author:** nextlevelshit

## Issue Body

## Context

From wave-scope-audit run wave-scope-audit-20260422-075324-5b6c.

Package inventory: `contract` — `keep (reorganize by contract type)`. Current layout groups code by operation (validate/recover/judge) rather than by the contract type each operation acts on, which spreads related logic across files.

## Acceptance Criteria

- [ ] `internal/contract` reorganized so each contract type owns its validation, recovery, and judge code
- [ ] Operation-grouped files collapsed or removed
- [ ] Package exported surface preserved (callers unaffected)
- [ ] Tests continue to pass

---
*Generated by wave-scope-audit pipeline*

## Acceptance Criteria (Extracted)

1. `internal/contract` reorganized so each contract type owns its validation, recovery, and judge code.
2. Operation-grouped files collapsed or removed.
3. Package exported surface preserved (callers unaffected).
4. Tests continue to pass (`go test ./internal/contract/...` and full suite).

## Metadata

- Branch: `1172-reorganize-contract-package`
- Complexity: medium
- Skipped speckit steps: specify, clarify
