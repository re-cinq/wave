# scope: replace internal/webui.EnabledFeatures global + init() registration with explicit feature registry

**Issue:** [re-cinq/wave#1167](https://github.com/re-cinq/wave/issues/1167)
**Labels:** `scope-audit`
**State:** OPEN
**Author:** nextlevelshit

## Context

From wave-scope-audit run wave-scope-audit-20260422-075324-5b6c.

Package inventory: `webui` — `active (bloated)` with action `refactor features global + Server struct`. `EnabledFeatures` is a package-level global mutated via `init()`; this is hostile to testing and to the "no global agent state" principle (Wave IS #2).

## Acceptance Criteria

- [ ] `EnabledFeatures` global removed
- [ ] `init()`-based registration removed
- [ ] Explicit feature registry passed into `Server` or injected at startup
- [ ] Tests can enable/disable features without mutating globals

---

*Generated by wave-scope-audit pipeline*
